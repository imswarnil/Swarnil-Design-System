---
title: Travel collection
layout: home
description: Trips — where, how long, how far, and the route across the card. Every one links to an itinerary, because the useful half of a travel page is the plan, not the photographs.
eyebrow: Collections · Travel
source: src/components/life.css
---
<section class="im-section im-wrap im-pt-sm im-section-sm">
	<p class="im-eyebrow">Collections · Travel · {{page.title}}</p>
	<p class="im-lede">{{page.description}}</p>
	<p class="doc-source"><code>{{page.source}}</code></p>
</section>

<div class="im-wrap-wide im-stack im-stack-lg">
	<header class="im-cinehero">
		{{!-- The docs ship no clip: nothing in this repository may fetch a video
		     from somewhere else, and a poster with no src is exactly what a
		     reader on a slow connection or with motion turned down sees. Put
		     the file in src= in a real theme. --}}
		<video poster="{{demo.shots.[0]}}" muted loop playsinline autoplay preload="none" aria-hidden="true"></video>
		<p class="im-cinehero-meta"><span>{{demo.trips.length}} trips</span><span>4 countries</span><span>Since 2023</span></p>
		<h1 class="im-cinehero-title">Where I have been</h1>
		<p class="im-cinehero-text">Trips, with the itinerary and what each day cost. Written down mostly so I can answer "how would you do it again" without inventing the answer.</p>
		<div class="im-cinehero-controls">
			<button type="button" aria-label="Mute" aria-pressed="true">{{icon "volume-x"}}</button>
			<button type="button" aria-label="Pause">{{icon "pause"}}</button>
		</div>
	</header>

	<section class="im-section im-section-tight">
		<div class="home-head"><p class="im-caption" data-index="latest">Most recent</p></div>
		{{#with demo.trips.[0]}}
		<article class="im-tripfeature">
			<div class="im-tripfeature-pic">
				<img src="{{../demo.shots.[0]}}" alt="" loading="eager">
				<span class="im-kind im-kind-over" data-kind="trip">{{icon "map-pin"}}{{country}}</span>
				<span class="im-post-card-featured" aria-label="Featured"><span aria-hidden="true">Featured</span></span>
			</div>
			<div class="im-tripfeature-body">
				<p class="im-tripcard-meta im-muted"><span>{{when}}</span><span>{{days}} days</span><span>{{km}} km</span></p>
				<h2 class="im-tripfeature-title"><a href="/collections/travel/trip/" style="color: inherit; text-decoration: none">{{title}}</a></h2>
				<p class="im-exp-text">{{text}}</p>
				<div class="im-tripfeature-days im-tripfeature-days-3">
					{{#each ../demo.itinerary}}<p class="im-tripfeature-day">{{#match kind "=" "start"}}{{icon "rocket"}}{{/match}}{{#match kind "=" "stay"}}{{icon "bookmark"}}{{/match}}{{#match kind "=" "road"}}{{icon "route"}}{{/match}}{{#match kind "=" "beach"}}{{icon "sun"}}{{/match}}{{#match kind "=" "climb"}}{{icon "trending-up"}}{{/match}}{{#match kind "=" "end"}}{{icon "house"}}{{/match}}<b>Day {{day}}</b><span>{{title}}</span><small>{{cost}}</small></p>{{/each}}
				</div>
			</div>
			<div class="im-tripfeature-foot">
				<a class="im-btn im-btn-primary im-btn-sm" href="/collections/travel/trip/">{{icon "route"}}The itinerary</a>
				<a class="im-btn im-btn-sm" href="/collections/video/page/">{{icon "play"}}The film</a>
			</div>
		</article>
		{{/with}}
	</section>

	<section class="im-section im-section-tight">
		<div class="home-head"><p class="im-caption" data-index="regions">By region</p></div>
		<div class="im-regions">
			{{#each demo.regions}}<a class="im-region" href="#trips"{{#if state}} data-state="{{state}}"{{/if}}><img src="{{lookup ../demo.shots shot}}" alt="" loading="lazy"><span class="im-kind im-kind-over" data-kind="trip">{{icon mark}}{{#if state}}Next{{else}}Been{{/if}}</span><span class="im-region-name">{{name}}</span><span class="im-region-meta">{{trips}} trips · {{countries}} countries</span></a>{{/each}}
		</div>
	</section>

	<section class="im-section im-section-tight">
		<div class="home-head"><p class="im-caption" data-index="countries">Countries</p><p class="im-hint im-m0">tick = been · blue = booked · dashed = yet to visit</p></div>
		<div class="im-countries">
			{{#each demo.countries}}<a class="im-country" href="#trips" data-state="{{state}}"><span class="im-country-mark" aria-hidden="true"></span><b>{{name}}</b><small>{{region}} · {{#match state "=" "next"}}not yet{{else}}{{days}} days{{/match}}</small><span class="im-country-state">{{#match state "=" "done"}}Been{{/match}}{{#match state "=" "booked"}}Booked{{/match}}{{#match state "=" "next"}}To visit{{/match}}</span></a>{{/each}}
		</div>
	</section>

	<section class="im-section im-section-tight">
		<div class="home-head"><p class="im-caption" data-index="trips">Every trip</p></div>
	<div class="im-collbar">
		<div class="im-collbar-filters" role="group" aria-label="Which country" data-im-filter="#trips">
			<button class="im-tag" type="button" value="all" aria-current="page">All</button>
			<button class="im-tag" type="button" value="Italy" aria-current="false">Italy</button>
			<button class="im-tag" type="button" value="Croatia" aria-current="false">Croatia</button>
			<button class="im-tag" type="button" value="Slovakia" aria-current="false">Slovakia</button>
			<button class="im-tag" type="button" value="Norway" aria-current="false">Norway</button>
		</div>
		<div class="im-cluster im-cluster-tight">
			<button class="im-btn im-btn-sm" type="button">{{icon "list-filter"}}Most recent</button>
		</div>
	</div>

	<div class="im-trips" id="trips" style="--im-trips-min: 17rem">
		{{#each demo.trips}}
		<a class="im-tripcard" href="/collections/travel/trip/" data-facet="{{country}}">
			<img src="{{lookup ../demo.shots @index}}" alt="" loading="lazy">
			<span class="im-kind im-kind-over" data-kind="tag">{{icon "map-pin"}}{{country}}</span>
			<span class="im-exp-figure">{{days}}<small>days</small></span>
			<span class="im-tripcard-body">
				<span class="im-tripcard-title">{{title}}</span>
				<span class="im-tripcard-meta"><span>{{when}}</span>{{#match state "=" "upcoming"}}<span>Planned</span>{{else}}<span>{{km}} km</span>{{/match}}</span>
				<span class="im-tripcard-more"><span>{{text}}</span><span class="im-tripcard-days">{{#each @root.demo.itinerary}}{{#match @index "<" 3}}<span>Day {{day}} · {{title}}</span>{{/match}}{{/each}}</span></span>
			</span>
		</a>
		{{/each}}
		<p class="im-feed-empty" data-facet-empty hidden>Nothing here yet.</p>
	</div>

	</section>
	<section class="im-section im-section-tight">
		<div class="home-head"><p class="im-caption" data-index="totals">In total</p></div>
		<div class="im-expstats">
			<span><strong>{{demo.trips.length}}</strong>Trips</span>
			<span><strong>4</strong>Countries</span>
			<span><strong>652 km</strong>By road</span>
			<span><strong>21</strong>Days away</span>
			<span><strong>1</strong>Booked</span>
		</div>
	</section>
</div>

<section class="im-section im-wrap"><div class="im-prose">
<p><code>im-tripcard</code>: a short 4:3 picture with the country and the days on it, the title and two facts on a dark scrim, and — on hover, where there is a hover — the line about it and the route. On a touch screen the facts are simply shown. The head is <code>im-cinehero</code>: a clip behind the words, poster first, nothing playing under reduced motion. Each trip opens an <a href="/collections/travel/trip/">itinerary</a>.</p>
</div></section>
