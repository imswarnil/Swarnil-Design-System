---
title: Guestbook demo
layout: home
shell: frame
---
{{!-- Loaded in an iframe by /pages/guestbook/. The explanation pinned on
	the left, the entries scrolling on the right, the way home at the top. --}}
<div class="im-solo im-solo-split im-solo-scroll im-solo-panel-top im-solo-lg">
	<aside class="im-solo-panel">
		<span class="im-solo-pattern im-bg-dots" aria-hidden="true"></span>
		<h1 class="im-collhead-title">The guestbook</h1>
		<p class="im-lede im-m0">Where you are and one line. No thread to follow — the page I reread most.</p>
		<ul class="im-widget-now">
			<li>{{> "icons/pencil"}}<span><b>Sign it</b> from the box on the right.</span></li>
			<li>{{> "icons/map-pin"}}<span><b>Say where</b> — that is the whole point.</span></li>
			<li>{{> "icons/circle-check"}}<span><b>Kept forever.</b> Entries are never pruned.</span></li>
		</ul>
		<div class="im-widget-stats"><div><strong>412</strong><span>Entries</span></div><div><strong>38</strong><span>Countries</span></div><div><strong>2019</strong><span>Since</span></div></div>
	</aside>

	{{#> "shell/solo-bar"}}{{/"shell/solo-bar"}}

	<main class="im-solo-main">
		{{> "components/comments" thread=demo.comments count="412"}}
	</main>

	{{> "shell/solo-foot"}}
</div>
