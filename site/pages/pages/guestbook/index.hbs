---
title: Guestbook
layout: home
description: What it is and why, fixed on the left; the entries on the right, which are Ghost's own comments on this page.
eyebrow: Pages
source: src/components/pages.css · comments.css
---
<section class="im-section im-wrap im-pt-sm im-section-sm">
	<p class="im-eyebrow">Pages · {{page.title}}</p>
	<p class="im-lede">{{page.description}}</p>
	<p class="doc-source"><code>{{page.source}}</code></p>
</section>

<div class="im-wrap-wide im-guestbook">
	<aside class="im-guestbook-intro">
		<h1 class="im-collhead-title">The guestbook</h1>
		<p class="im-lede im-m0">Where you are and one line. No accounts to make, no thread to follow — the page I reread most.</p>
		<ul class="im-widget-now">
			<li>{{icon "pencil"}}<span><b>Sign it</b> from the box on the right; it is this page's own comment thread.</span></li>
			<li>{{icon "map-pin"}}<span><b>Say where</b> — that is the whole point of a guestbook.</span></li>
			<li>{{icon "circle-check"}}<span><b>Kept forever.</b> Entries are never pruned.</span></li>
		</ul>
		<div class="im-widget-stats"><div><strong data-im-count>412</strong><span>Entries</span></div><div><strong data-im-count>38</strong><span>Countries</span></div><div><strong>2019</strong><span>Since</span></div></div>
	</aside>
	<div class="im-guestbook-wall im-guestbook-comments">
		{{> "components/comments" thread=demo.comments count="412"}}
	</div>
</div>

<section class="im-section im-wrap"><div class="im-prose"><p>In a theme the right column is <code>{{"{{comments}}"}}</code> on a page called Guestbook. The left is sticky and explains; it never asks for anything the comment box does not.</p></div></section>
