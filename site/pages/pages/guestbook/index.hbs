---
title: Guestbook
layout: home
description: What it is and why, fixed on the left; the entries on the right, which are Ghost's own comments on this page.
eyebrow: Pages
source: src/layout/solo.css · comments.css
---
<section class="im-section im-wrap im-pt-sm im-section-sm">
	<p class="im-eyebrow">Pages · {{page.title}}</p>
	<p class="im-lede">{{page.description}}</p>
	<p class="doc-source"><code>{{page.source}}</code></p>
</section>

<section class="im-section im-wrap-wide">
	<p class="im-cluster"><a class="im-btn im-btn-primary im-btn-sm" href="/demos/guestbook/">Open full screen {{icon "arrow-up-right"}}</a><span class="im-hint">A page of its own: <code>im-solo</code>, the way home in the bar.</span></p>
	<iframe class="demo-frame im-demo-tall" src="/demos/guestbook/" title="The guestbook" loading="lazy"></iframe>
	<iframe class="demo-frame im-max-sm im-mx-auto im-block im-demo-tall" src="/demos/guestbook/" title="The guestbook on a phone" loading="lazy"></iframe>
</section>

<section class="im-section im-wrap"><div class="im-prose"><p>In a theme the right column is <code>{{"{{comments}}"}}</code> on a page called Guestbook. The left is the <code>im-solo-split</code> panel, pinned; it explains and never asks for anything the comment box does not. On a phone it stays, as a band over the entries (<code>im-solo-panel-top</code>).</p></div></section>
