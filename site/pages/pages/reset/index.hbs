---
title: Forgot password
layout: home
description: Ghost has no passwords, so this page's job is to say so — and to send the link anyway.
eyebrow: Pages
source: src/components/auth.css
---
<section class="im-section im-wrap im-pt-sm im-section-sm">
	<p class="im-eyebrow">Pages · {{page.title}}</p>
	<p class="im-lede">{{page.description}}</p>
	<p class="doc-source"><code>{{page.source}}</code></p>
</section>

<section class="im-section im-wrap-wide">
	<p class="im-cluster"><a class="im-btn im-btn-primary im-btn-sm" href="/demos/reset/">Open full screen {{icon "arrow-up-right"}}</a><span class="im-hint">A page of its own: <code>im-solo</code>, the way home in the bar.</span></p>
	<iframe class="demo-frame im-demo-tall" src="/demos/reset/" title="Reset" loading="lazy"></iframe>
	<iframe class="demo-frame im-max-sm im-mx-auto im-block im-demo-tall" src="/demos/reset/" title="Reset on a phone" loading="lazy"></iframe>
</section>

<section class="im-section im-wrap"><div class="im-prose">
<p>Ghost signs members in by link, so "forgot password" and "sign in" are the same request with a different heading. The heading matters: somebody who arrives here is worried, and the first sentence has to remove the worry before it asks for anything. <code>im-solo</code> centres the card in the window with nothing else on it but the way back.</p>
</div></section>
