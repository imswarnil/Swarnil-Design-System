# Creator icons

24x24 grid, 1.5px stroke, `currentColor` only — every icon follows the text
colour, so dark/light theming is automatic. Groups: ui/ creator/ media/ social/.
Use inline (paste the file) or as <img src> (inherits nothing — prefer inline
or a sprite when you need theming).
