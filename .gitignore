node_modules/
.DS_Store
*.log

# dist is committed on release so the CDN and GitHub Pages always have it;
# it is regenerated by `npm run build`.

# Mirrored into docs/ by `npm run docs` so every path is web-root relative.
docs/src/
docs/icons/
docs/dist/

# Python bytecode
__pycache__/
*.pyc
docs/collection/
